### Phase 6: Working Demo  
Keep adding individual game elements in a "test data" format.  

- Build out some SLIGHT story elements.  fill out the dialogue options with npc's, spice up the descriptions, give the characters real names
  - quests
    - add a quest to region 2
      - find a way to make that quest 'beat the game'
      -also build out region 2 dialogue trees here
    - add a side quest to region 1
      - and build out region 1 dialogue further based on this
  - region 2
    - update regionadversary so that region 2 represents an increase in difficulty
    - update all characters for the same purpose
    - update room descriptions, item descriptions, npc descriptions, everything
  - survival
    - add at least a food and water item 
    - make sure that hunger and thirst states are working
  - credits
    - create a credits screen and methods to launch that on game completion (beating the quest in region 2)
  - replace all instances of "working" or other such test text (except for persistence)
- dialogue is getting kinda tricky.  you can't really have npc's always start at the same spot necessarily.  that's fine for generic npc's, but quest givers? they need to update their starting state based on what you have done...
- make sure the same quest can't be assigned twice (this only actually matters for side quests, but still)
- add a screen for loading new regions to the load region section.  this will require an update to both region json's to add a field for the introduction.  there should be sleep functions involved.  the region 1 intro can be the game intro, which will mean pulling some of the corrent introduction.json out into a separate file for region intro
- Reassess demo at this time, have stoobics test the game.  
  - In the planning stage, currently seems like this is a full demo.  
  - Check **ALL** TODO comments in every file.  
    - that means making all of the specific items mentioned in todo's including all the traps (5 to go) and the things you get from traps
- (Assuming that the demo is in fact done) host the demo.  
  - Find out what changes need to be made to host the demo game online.   
    - If that isn't reasonable, update the README to provide a detailed description of running the game.  
  - Tell friends to test it.  

### NEXT PHASES  

- **Phase 7: Codebase Creation**  
  - Create files for all items.  
    - If the deserializer gives you issues, put JSON methods in the switch statements rather than before it (see `CharacterDeserializer`).  
    - Update item manual
    - add item creation to the crafting menu (see Menu.java todos)
  - And all spells.  
  - And all NPCs.  
  - And everything and anything else.  
    - Pay close attention to the conveyance. It'll be basically an NPC, I guess. It will have an inventory with functionally no weight limit. 

- **Phase 8: Persistence**  
  - Create methods for saving and loading game states to test persistence.  

- **Phase 9: The Actual Game Itself**  
  - Start coding the levels one-by-one, modularly add them to the game.  